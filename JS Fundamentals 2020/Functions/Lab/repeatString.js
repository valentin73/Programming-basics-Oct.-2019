function repeatStrings(string, repetitions){
    let result = string.repeat(repetitions);
    return result;
}
let returnedValue = repeatStrings('abc', 3)
console.log(returnedValue);
repeatStrings('abc',3)